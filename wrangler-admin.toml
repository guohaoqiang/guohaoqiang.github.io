# Wrangler configuration for Admin Proxy Worker
# This worker protects /admin.html with JWT token verification

name = "admin-proxy"
main = "cloudflare-worker/worker-rs256-fixed.js"
compatibility_date = "2025-12-21"
type = "javascript"

account_id = "7ed5db77a991f01677e08d93fd670e1c"

# Deployment:
# 1. Ensure ADMIN_TOKEN secret is set:
#    wrangler secret put ADMIN_TOKEN
# 2. Publish: `wrangler publish --config wrangler-admin.toml`
